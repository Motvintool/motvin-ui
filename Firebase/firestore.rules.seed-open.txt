rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary during automated seed only.
    match /products/{docId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
